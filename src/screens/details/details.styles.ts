import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1
    },
    imageContainer: {
        flex: 0.5
    },
    contentContainer: {
        flex: 0.5
    },
    headerText: {
        marginLeft: 50,
        marginTop: 10,
        fontSize: 34,
        color: "#FFF"
    }
});

export default styles;
